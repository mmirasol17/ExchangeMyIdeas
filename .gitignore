# Local database credentials -- never commit
config.local.php

# Claude Code local settings -- never commit
.claude/settings.local.json

# OS cruft
.DS_Store
Thumbs.db

# Runtime markers written by the auto-migrator (server-side state, not source)
migrations/.applied
migrations/.failed
