# Block direct web access to raw migration SQL files.
# The migrate.php runner reads them from the server filesystem, so it is
# unaffected by this rule.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
